<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<%= render @users %>

	<!--
	moze i taka mesto gornata edna linija
	<%  @users.each do |user|	%>
		<%= render user %> 
	<% end %>

</ul>
-->

<%= will_paginate %>